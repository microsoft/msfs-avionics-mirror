/**
 * The events that the EIS engine page can send to the EIS system.
 */
export interface G3XEisEvents {
  /**
   * Whether the EIS engine page is open.
   */
  g3x_eis_engine_page_is_open: boolean;
}